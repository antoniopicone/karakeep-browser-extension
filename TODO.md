- [x] adding a link does not trigger list reload — root cause was a `chrome.runtime.connect` port used to track "is the panel open", which goes stale whenever Chrome suspends/respawns the service worker; replaced with a direct `sendMessage` + fallback
- [x] remote deletions (from another synced device) now also update an open panel's list, not just additions
- [x] ~~adapted to serverless-sync's new protocol — register-then-encrypted-envelope (ChaCha20-Poly1305) per application~~ — superseded by the next item; that HTTP+crypto client (`syncd-client.js`, `vendor/chacha20poly1305.js`) is gone
- [x] replaced the generic, separately-installed syncd (talked to over HTTP, port + pairing secret in Settings) with an embedded, single-purpose daemon (`native/reading-list-syncd/`) reached via Chrome Native Messaging instead — no port/secret to configure anymore. The reading list now lives in a plain CSV ledger (`~/.reading-list/reading-list.csv`) that's readable/editable outside the extension. See the top-level README and `native/reading-list-syncd/README.md`.
- [ ] peer-to-peer traffic between `reading-list-syncd` instances is unencrypted (relies on Tailscale's transport encryption) — bring back a `crypto.rs`-style envelope if this ever needs to run over an untrusted LAN
- [ ] report the seq/version-vector replay bug found and fixed in `native/reading-list-syncd/src/core.rs` (see its README) upstream to serverless-sync
